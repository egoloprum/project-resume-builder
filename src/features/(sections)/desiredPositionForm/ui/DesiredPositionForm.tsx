'use client'

import { FC } from 'react'

interface DesiredPositionFormProps {}

export const DesiredPositionForm: FC<DesiredPositionFormProps> = ({}) => {
  return <div className="p-4 border-1 my-4">DesiredPositionForm</div>
}
