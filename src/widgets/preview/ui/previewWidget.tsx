'use client'

import { FC } from 'react'

interface PreviewWidgetProps {}

export const PreviewWidget: FC<PreviewWidgetProps> = ({}) => {
  return <div className="p-4 border-2 w-full min-h-screen">PreviewWidget</div>
}
